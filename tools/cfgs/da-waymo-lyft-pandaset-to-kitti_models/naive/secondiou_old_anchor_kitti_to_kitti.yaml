_BASE_CONFIG_: cfgs/da-waymo-lyft-pandaset-to-kitti_models/naive/secondiou_old_anchor_common.yaml

CLASS_NAMES: ['Car']
ONTOLOGY: 'kitti'

DATA_CONFIGS:
    KITTI_CONFIG:
        _BASE_CONFIG_: cfgs/dataset_configs/da_kitti_dataset.yaml
        FOV_POINTS_ONLY: True
        CLASS_NAMES: ['Car']
        SHIFT_COOR: [0.0, 0.0, 1.6]

DATA_CONFIG_TAR:
    _BASE_CONFIG_: cfgs/dataset_configs/da_kitti_dataset.yaml
    FOV_POINTS_ONLY: True
    CLASS_NAMES: ['Car']
    SHIFT_COOR: [0.0, 0.0, 1.6]
